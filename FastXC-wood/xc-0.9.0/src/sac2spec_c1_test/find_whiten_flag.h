#ifndef FIND_WHITEN_FLAG_H
#define FIND_WHITEN_FLAG_H

#include <stdio.h>
void find_whiten_flag(int whitenType, int normalizeType, int *wh_before,
                      int *wh_after, int *do_runabs, int *do_onebit);

#endif