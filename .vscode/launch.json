{
  // 使用 IntelliSense 了解相关属性。 
  // 悬停以查看现有属性的描述。
  // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
      {
          "name": "CUDA C++: Launch",
          "type": "cuda-gdb",
          "request": "launch",
          "program": "${fileDirname}/test",
          "preLaunchTask": "CUDA: nvcc 调试"
      },
      {
          "name": "CUDA C++: Attach",
          "type": "cuda-gdb",
          "request": "attach"
      },
      {
          "name": "CUDA编译并运行",
          "type": "cuda-gdb",
          "request": "launch",
          "program": "${fileDirname}/${fileBasenameNoExtension}",
          "preLaunchTask": "CUDA: nvcc 编译并运行"
      }
  ]
}